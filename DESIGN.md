# DESIGN.md — 롤 내전 팀툴 디자인 시스템

모든 UI 작업은 이 문서를 기준으로 한다. 여기에 없는 색·크기·컴포넌트를 임의로 만들지 않는다.
새 값이 필요하면 **먼저 이 문서에 등록한 뒤** 코드에 반영한다.

## 1. 컬러 시스템

### 1.1 기본 팔레트 (사용자 지정 4색)

| 구분 | 색상 | 역할 |
|---|---|---|
| 라이트 | `#F8FAFC` | 페이지 배경 |
| 라이트 | `#D9EAFD` | 카드/패널 배경 |
| 라이트 | `#BCCCDC` | 테두리, 드래그 호버 |
| 라이트 | `#9AA6B2` | 비활성·장식 요소 (텍스트에 사용 금지 — 대비 부족) |
| 다크 | `#352F44` | 페이지 배경 |
| 다크 | `#5C5470` | 테두리, 드래그 호버 |
| 다크 | `#B9B4C7` | 보조 텍스트, 강조(accent) |
| 다크 | `#FAF0E6` | 본문 텍스트, 주요 버튼 배경 |

### 1.2 파생 색 (가독성 확보용)

기본 팔레트만으로 명도 대비가 부족한 경우, **같은 색조(Hue)에서 명도만 조절한 파생색**을 허용한다.
파생색은 반드시 아래 표에 등록하고 사용한다.

| 색상 | 유래 | 역할 |
|---|---|---|
| `#2B3644` | `#9AA6B2` 어둡게 | 라이트 모드 본문 텍스트 |
| `#4A5764` | `#9AA6B2` 어둡게 | 라이트 모드 이름 칩 배경, accent |
| `#6B7A89` | `#9AA6B2` 어둡게 | 라이트 모드 보조 텍스트(placeholder) |
| `#453D57` | `#352F44`↔`#5C5470` 중간 | 다크 모드 카드/패널 배경 |

### 1.3 시맨틱 컬러

의미 전달용으로만 사용한다. 티어 색은 테마와 무관하게 고정, 팀 진영 색은 테마별로 명도를 조정한다.

| 용도 | 토큰 | 라이트 | 다크 |
|---|---|---|---|
| 티어 상 | `상` | `#52B788` | `#52B788` |
| 티어 중 | `중` | `#0077B6` | `#0077B6` |
| 티어 하 | `하` | `#F7B801` | `#F7B801` |
| 블루팀 | `teamBlue` | `#0077B6` | `#4DA8DA` |
| 레드팀 / 삭제·오류 | `teamRed` | `#E63946` | `#F07178` |

### 1.4 테마 토큰 매핑

컴포넌트에서는 hex를 직접 쓰지 않고 **반드시 테마 토큰**(`${({ theme }) => theme.xxx}`)을 쓴다.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `body` | `#F8FAFC` | `#352F44` | 페이지 배경 |
| `text` | `#2B3644` | `#FAF0E6` | 본문 텍스트 |
| `card` | `#D9EAFD` | `#453D57` | 카드/패널 배경 |
| `cardBorder` | `#BCCCDC` | `#5C5470` | 카드 테두리 |
| `placeholder` | `#6B7A89` | `#B9B4C7` | 보조 텍스트, 레이블 |
| `dragOver` | `#BCCCDC` | `#5C5470` | 드래그 호버, 버튼 호버 배경 |
| `nameBg` / `nameText` | `#4A5764` / `#F8FAFC` | `#FAF0E6` / `#352F44` | 플레이어 이름 칩 |
| `contextMenu` / `contextMenuBorder` | `#F8FAFC` / `#BCCCDC` | `#453D57` / `#5C5470` | 우클릭 메뉴 |
| `accent` | `#4A5764` | `#B9B4C7` | 강조 텍스트, 포커스 |
| `accentGradient` | `#6B7A89→#4A5764` | `#FAF0E6→#B9B4C7` | 주요 버튼, 활성 탭 배경 |
| `accentText` | `#F8FAFC` | `#352F44` | accentGradient 위 텍스트 |

## 2. 레이아웃

### 2.1 레이아웃 토큰 (CSS 변수, GlobalStyle에 정의)

| 변수 | 기본 | 높이 780px 이하 | 용도 |
|---|---|---|---|
| `--control-h` | 40px | 36px | 이름 칩·슬롯 높이 |
| `--section-gap` | 1rem | 0.625rem | 섹션 간 간격 |
| `--lane-gap` | 0.75rem | 0.5rem | 라인 카드 간 간격 |
| `--radius-lg` | 12px | - | 카드, 입력창 |
| `--radius-md` | 8px | - | 칩, 슬롯, 탭 버튼 |
| `--radius-sm` | 6px | - | 작은 버튼, 메뉴 항목 |

### 2.2 무스크롤 규칙 (팀 빌딩 화면)

- **팀 빌딩 탭은 어떤 화면 높이에서도 세로 스크롤이 생기면 안 된다.**
  `AppContainer`에 `$fitViewport`를 주면 `height: 100vh; overflow: hidden`이 적용된다.
- 팀 빌딩 탭에 요소를 추가할 때는 고정 높이 대신 위 CSS 변수를 사용해 화면 높이에 따라 축소되게 한다.
- 내전 기록 등 다른 탭은 세로 스크롤 허용.

### 2.3 페이지 구조

```
PageWrapper (가운데 정렬, 좌우 광고 사이드바 160px — 1200px 미만에서 숨김)
└─ AppContainer (max-width 800px)
   ├─ AppHeader (테마 토글, absolute)
   ├─ TabBar (팀 빌더 | 내전 기록)
   └─ 탭 콘텐츠
```

## 3. 컴포넌트 규칙

새 UI는 반드시 `App.styles.ts`의 공용 컴포넌트에서 시작한다. 같은 역할의 스타일을 컴포넌트 파일에 새로 만들지 않는다.

| 컴포넌트 | 용도 |
|---|---|
| `Card` | 모든 패널·박스의 기본. 확장은 `styled(Card)` |
| `PrimaryButton` | 화면당 1개의 주요 행동 (accentGradient 배경) |
| `SecondaryButton` | 취소·보조 행동 (테두리형) |
| `CompactButton` | 목록/행 안의 작은 행동 (추가·복사·삭제 등) |
| `TextField` | 텍스트 입력. 배경은 `body` 토큰 (카드 위에서 구분되도록) |
| `ui/Select` | 커스텀 드롭다운 (네이티브 select 사용 금지) |
| `ui/DatePicker` | 커스텀 날짜 선택 (네이티브 date input 사용 금지, 값 형식 YYYY-MM-DD) |
| `ModalOverlay` + `ModalContent` | 모든 모달. 오버레이 클릭 시 닫힘 + 내부 클릭 전파 차단 |
| `TabBar` + `TabButton` | 화면 전환 탭 |
| `DraggableName` | 플레이어 이름 칩 (티어 색 테두리) |
| `FooterBar` | 우하단 고정 정적 페이지 링크 (가이드·개인정보처리방침) |

## 4. 타이포그래피

- 폰트: 시스템 폰트 스택 (`-apple-system, ... sans-serif`), 기본 16px
- 본문 1rem / 보조 0.9~0.95rem / 패널 제목 1.1rem(h3, 하단 구분선) / 모달 제목 1.4rem
- 강조는 `font-weight: 600` 이상 + `accent` 색으로. 이탤릭 사용 금지

## 5. 가독성(접근성) 기준

- 본문 텍스트 ↔ 배경 대비 **4.5:1 이상** (`text` 토큰은 이 기준으로 선정됨)
- 보조 텍스트·placeholder는 최소 3:1
- `#9AA6B2`(라이트), `#5C5470`(다크)는 텍스트 색으로 사용 금지 — 배경/테두리 전용

## 6. 금지 사항

- 컴포넌트 파일에 hex 컬러 하드코딩 금지 (시맨틱 티어 색 포함 — 전부 테마 토큰 경유)
- 인라인 `style={{...}}` 은 동적 좌표 등 불가피한 경우만
- 팀 빌딩 탭에 스크롤을 유발하는 고정 높이 요소 추가 금지
- 새 border-radius, 새 그림자 값 임의 추가 금지 (기존 토큰 재사용)
